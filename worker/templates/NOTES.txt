The {{ include "worker.name" . }} worker has been deployed with {{ .Values.app.replicas }} replica{{ if ne (int .Values.app.replicas) 1 }}s{{ end }}.

This template does not create a Service, ingress, or TLS resources.
