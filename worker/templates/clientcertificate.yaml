# Podplane <https://podplane.dev>
# Copyright The Podplane Authors
# SPDX-License-Identifier: Apache-2.0

{{- if and .Values.certificates.client .Values.certificates.secrets }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "worker.name" . }}-client-certificate
  labels:
    {{- include "worker.labels" . | nindent 4 }}
spec:
  secretName: {{ include "worker.clientCertificateSecretName" . }}
  dnsNames:
    - {{ include "worker.clientCertificateDNSName" . | quote }}
  usages:
    - digital signature
    - client auth
  issuerRef:
    kind: ClusterIssuer
    name: platform-service-certificate-clusterissuer
{{- end }}
